# OS
.DS_Store
**/.DS_Store

# Secrets & Node deps (Razorpay serverless functions)
.env
.env.*
!.env.example
node_modules/
.vercel

# Source originals — NOT used by the site (photos are copied/compressed into
# assets/products/ and the hero into assets/video/). ~2GB, keep out of git.
le-rosea images/

# Unreferenced source media / internal documents
video2.mov
*.mov
Product name.pdf
Emb details.pdf

# Internal pricing sheets (not part of the public site)
pricing.png
pricing1.png

# Standalone size chart image (not wired into the site)
SIZE_CHART.png

# Full-res campaign originals — optimized copies live in assets/products/
sequinsrushtrio.jpeg
fringeminitrio.jpeg

# Raw unencoded footage
craftsman.MP4

# Local test scratch files (root-level only, so nested files like
# api/_razorpay-config.js are still tracked)
/_*
lol.png


# Large source video bundle (only transcoded copies belong in assets/video/products/)
website_videos.zip
*.zip
website_videos/
Website videos/

# Downloaded app installers / disk images (never part of the site)
*.dmg
